psutil>=5.9.0

# Build (6.22+ required: earlier versions cannot bundle Tcl 9 on Python 3.13+)
pyinstaller>=6.22.0

# Releases are published with the GitHub CLI (winget install GitHub.cli),
# and updater.py is stdlib-only by design, so there is nothing else to install.
